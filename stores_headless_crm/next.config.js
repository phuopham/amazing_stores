/** @type {import('next').NextConfig} */

// TODO:modify it to newer style per Next document
const nextConfig = {
    images: {
        domains: [
            'res.cloudinary.com'
        ]
    }
}

module.exports = nextConfig
